[project]
name = "m1k_utils"
version = "0.1.0"
description = "Utilities for ADALM1000 (M1K)"
authors = [
    { name = "Ashwin Prasanth Hariharan" }
]
dependencies = [
    "pysmu"
]
requires-python = ">=3.9"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"